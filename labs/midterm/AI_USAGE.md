# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** ______________________ **รหัส:** ______________ **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 15:20 | B1.3 | บั๊ก 3 — กดตัวกรองสถานะใด ๆ (เช่น “เสร็จสิ้น”) แล้วได้ผลเหมือนเดิม (เหมือนกดตัวกรองไม่ทำงาน) โค้ดคือ <FilterBar onFilterChange="{setStatusFilter}" value="{statusFilter}"/> | ปัญหาเกิดจากการที่ผูก State ไว้ที่ปุ่มตัวกรองแล้ว แต่ตรงจุดที่นำข้อมูลมาแสดงผล (ลูป .map) ไม่ได้นำ State นั้นมาใช้คัดกรองข้อมูล ทำให้แอปยังคงแสดงรายการทั้งหมดเสมอ วิธีแก้คือให้แทรกคำสั่ง .filter() เพื่อตรวจสอบเงื่อนไข statusFilter ก่อนที่จะนำอาร์เรย์ไป .map()| ไล่หาตัวที่เรียกFilterBarแล้วไล่หาที่ตรวจสอบเงื่อนไขว่าstatusFilterได้ตรวจสอบรึยัง |
| 15:50 | B1.4 | หน้าเว็บภาษาReact Router พอเปิดหน้าดูรายละเอียดข้อมูล เช่น URL เป็น #/requests/REQ-101 แล้วลองพิมพ์แก้ URL บนเบราว์เซอร์เปลี่ยนเป็น REQ-102 พอเอนเทอร์แล้วข้อมูลบนหน้าจอดันไม่เปลี่ยนตาม (ค้างข้อมูลของ 101 ไว้) อาการแบบนี้เกิดจากสาเหตุอะไรได้บ้าง? เกี่ยวกับการเขียน useEffect หรือลืมใส่ Dependency Array ไหม? | อาการที่ URL เปลี่ยนแต่หน้าจอไม่ยอมอัปเดต เป็นพฤติกรรมปกติของ React Router ครับ เมื่อเราเปลี่ยนแค่ "พารามิเตอร์" ด้านหลัง (จาก 101 เป็น 102) แต่ยังอยู่หน้าเว็บเดิม ตัวคอมโพเนนต์จะ ไม่ถูกรีเฟรชหรือสร้างใหม่ No Remount | บล็อกคำสั่ง useEffect ที่ใช้useEffect เพื่อดึงข้อมูล REQ-101 มาแสดงในตอนแรก แต่ใน [] ที่อยู่บรรทัดสุดท้ายของ useEffect ไม่ได้ใส่ตัวแปร ID ของ URL เอาไว้ |
| 16:25 | B1.6 | ใน React กดปุ่มลบแล้วหน้าเว็บพังเป็นจอขาวและ Console ขึ้น Error เกิดจากการเขียนฟังก์ชันใน onClick ผิดวิธี | อาการหน้าพังทันทีเมื่อกดปุ่มมักเกิดจากการใส่เครื่องหมายวงเล็บเรียกฟังก์ชันทันทีที่เรนเดอร์ (onClick={func()}) แทนที่จะใช้ฟังก์ชันลูกศร (onClick={() => func()}) หรือเกิดจากการเรียกใช้ข้อมูลที่หลุดเป็น null/undefined | สาเหตุของ Error จึงได้ไปตรวจสอบโค้ดจุดที่มีการเรียกฟังก์ชัน handleDelete และการส่ง props |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |

*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ______________________
